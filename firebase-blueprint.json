{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "description": "User account and profile information",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "name": { "type": "string" },
        "email": { "type": "string" },
        "role": { "type": "string", "enum": ["freelancer", "business"] },
        "roleTitle": { "type": "string" },
        "rateOrBudget": { "type": "string" },
        "category": { "type": "string" },
        "bio": { "type": "string" },
        "skills": { "type": "array", "items": { "type": "string" } },
        "profileCompleted": { "type": "boolean" },
        "avatarInitials": { "type": "string" },
        "verified": { "type": "boolean" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "name", "role", "profileCompleted"]
    },
    "Gig": {
      "title": "Gig",
      "description": "Gig or talent listing",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "client": { "type": "string" },
        "clientId": { "type": "string" },
        "category": { "type": "string" },
        "tags": { "type": "array", "items": { "type": "string" } },
        "desc": { "type": "string" },
        "rate": { "type": "string" },
        "unit": { "type": "string" },
        "hot": { "type": "boolean" },
        "proposals": { "type": "string" },
        "posted": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["title", "category", "rate"]
    },
    "Swipe": {
      "title": "Swipe",
      "description": "Swipe action records",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "itemId": { "type": "string" },
        "direction": { "type": "string", "enum": ["left", "right", "up"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "itemId", "direction"]
    },
    "Match": {
      "title": "Match",
      "description": "User matched gig or freelancer with messaging thread",
      "type": "object",
      "properties": {
        "id": { "type": "number" },
        "userId": { "type": "string" },
        "job": { "type": "object" },
        "expiresAt": { "type": "number" },
        "messages": { "type": "array" },
        "matchedAt": { "type": "string" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "userId", "job", "expiresAt"]
    }
  },
  "firestore": {
    "rules": "firestore.rules"
  }
}
